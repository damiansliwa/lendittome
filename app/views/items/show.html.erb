<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit", edit_item_path(@item), class: 'btn btn-primary' %>
  <%= link_to "Show all", items_path, class: 'btn btn-primary' %>
  <%= link_to "Delete", item_path(@item), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
</div>